import styled from "styled-components";

const StyledList = styled.ul`
    display: flex;
    flex-direction: row;
    list-style: none;
    gap: 5em;
    margin-right: 2em;
    cursor: pointer;
    font-weight: bold;
`

export default StyledList